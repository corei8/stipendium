from setuptools import setup

setup(
        name='stipendium',
        packages=['stipendium'],
        include_package_data=True,
        install_requires=[
            'flask',
            ],
        )
